- name: Create a jenkins job using the token
  community.general.jenkins_job:
    config: "{{ lookup('template', '/template/pipeline.xml') }}"
    name: test
    url: "http://172.31.13.132.online:8080"
    user: "admin"
    password: 123456
  loop:
      - { repoName: "cart", componentName: "cart" }







